import { createSlice } from "@reduxjs/toolkit";

/* 

loginStep
signin, signup 관리 리덕스 

*/

const initialState = {
  currentStep: 1,
  nickName: undefined,
  profile: undefined,
};

const loginstepSlice = createSlice({
  
})